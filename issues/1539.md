---
number: 1539
title: "Clarify differences between `/devices` and `/keys/query`"
state: "open"
author: "richvdh"
created_at: "2023-05-24T13:44:54Z"
updated_at: "2023-05-24T14:52:26Z"
url: "https://github.com/matrix-org/matrix-spec/issues/1539"
labels:
  - "clarification"
---

Both [`POST /keys/query`](https://spec.matrix.org/v1.6/client-server-api/#post_matrixclientv3keysquery) and [`GET /devices`](https://spec.matrix.org/v1.6/client-server-api/#get_matrixclientv3devices) return a list of devices, so it's not entirely clear what the differences are.

The primary differences are:

* `/keys/query` returns only those devices which have uploaded encryption keys, whereas `/devices` returns *all* devices. This in particular is slightly subtle and the spec makes no reference to it.
* `/devices` cannot be used to query the device list of another user (it is strictly for the use of the local user to populate a "where am I logged in?" dialogue) whereas `/keys/query` can be used to get device information for other users


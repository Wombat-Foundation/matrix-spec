---
number: 15173
title: "Many endpoints that serialize events don't include the `transaction_id`"
state: "closed"
author: "sandhose"
created_at: "2023-02-28T16:59:27Z"
updated_at: "2023-03-06T16:08:41Z"
url: "https://github.com/matrix-org/synapse/issues/15173"
labels:
  - "A-Spec-Compliance"
  - "S-Minor"
  - "T-Defect"
  - "O-Frequent"
---

According to the spec, there are many endpoints that should include the `transaction_id` in the `unsigned` section of the event. Those endpoints are:

```sh
/sync
/rooms/{roomId}/state
/rooms/{roomId}/state/{eventType}/{stateKey}
/rooms/{roomId}/context/{eventId}
/rooms/{roomId}/messages
/rooms/{roomId}/relations/{eventId}
/rooms/{roomId}/relations/{eventId}/{relType}
/rooms/{roomId}/event/{eventId}
/rooms/{roomId}/threads
/search
/rooms/{roomId}/initialSync # Deprecated
/events # Deprecated
```

Synapse only does that on the `/sync` endpoint. I have [a Complement test]( https://github.com/matrix-org/complement/blob/55a17bbb4de541ee7bea9696459747e07af4e032/tests/csapi/txnid_test.go#L42) to highlight that issue using the `/rooms/{roomId}/event/{eventId}` endpoint.

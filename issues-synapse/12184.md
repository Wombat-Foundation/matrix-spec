---
number: 12184
title: "Duplicate `txn.call_after` calls when transactions are retried"
state: "closed"
author: "squahtx"
created_at: "2022-03-08T19:19:13Z"
updated_at: "2022-03-29T11:31:05Z"
url: "https://github.com/matrix-org/synapse/issues/12184"
labels:
  - "S-Tolerable"
  - "T-Defect"
  - "P4"
---

Synapse provides the ability for database transactions to defer some processing until after the transaction succeeds or fails, via the [`Cursor.call_after` and `Cursor.call_on_exception` methods](https://github.com/matrix-org/synapse/blob/release-v1.53/synapse/storage/database.py#L241-L259). These methods append the given callback to an internal list which [gets run once the transaction is done](https://github.com/matrix-org/synapse/blob/release-v1.53/synapse/storage/database.py#L755-L759).

When a transaction fails with a SerializationFailure, Synapse will retry it up to 5 times (inside [`new_transaction`](https://github.com/matrix-org/synapse/blob/release-v1.53/synapse/storage/database.py#L514)). Although a new Cursor is created for each attempt, the same `after_callbacks` list is reused. This means that if a transaction fails, then succeeds on the next attempt, the `after_callbacks` from the previous failed attempt will also run.

These callbacks are mostly used for cache invalidation, so this is probably okay?

-------------------

Note that [MultiWriterIdGenerator needs either the `call_after` or `call_on_exception` callbacks from failed transaction attempts to run](https://github.com/matrix-org/synapse/blob/release-v1.53/synapse/storage/util/id_generators.py#L542-L543), otherwise it wouldn't know when the ids handed out to failed transactions are done. If we were to clear out `after_callbacks` in between attempts, MultiWriterIdGenerator would break.

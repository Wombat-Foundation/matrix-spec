---
number: 1035
title: "delete database entries with lost foreign entries"
state: "closed"
author: "rubo77"
created_at: "2016-08-22T06:02:38Z"
updated_at: "2016-09-28T16:20:34Z"
url: "https://github.com/matrix-org/synapse/issues/1035"
labels: []
---

we deleted room history with

```
delete from events where room_id='$ROOM' and origin_server_ts < ((strftime('%s', 'now')-560400) *1000 ) and type='m.room.message' order by origin_server_ts
```

now  lot of entries in the database table `state_groups_state` that refer to an event that was deleted earlyer are lost

These are the tables that should have been pruned:

```
    #     +
    #     event_backward_extremities
    #     event_content_hashes
    #     event_destinations
    #     event_edge_hashes
    #     event_edges
    #     event_forward_extremities
    #     event_json
    #     event_push_actions
    #     event_reference_hashes
    #     event_search
    #     event_signatures
    #     event_to_state_groups
    #     events
    #     rejections
    #     room_depth
    #     state_groups
    #     state_groups_state
```

But we did only delete `events`. How do I delete all redundant entries now? (In my case there are still over 200.000 entries in state_groups_state for that room)


---
number: 14288
title: "Can't read SSL Certificate private key inside docker container"
state: "closed"
author: "TafkaMax"
created_at: "2022-10-25T10:31:59Z"
updated_at: "2023-01-18T14:28:14Z"
url: "https://github.com/matrix-org/synapse/issues/14288"
labels:
  - "A-Docker"
  - "T-Other"
  - "X-Needs-Info"
---

# Getting the Cert

I get my cert with certbot -> save it to /etc/letsencrypt. It gets the 0640 treatment and ```chown root:ssl-cert``` with a post-hook.

Create symlink to /etc/ssl/certs and /etc/ssl/private (644 and 640) respectivaly.

I mount them to /etc/ssl/cert and /etc/ssl/private inside the synapse docker container.

I create a matrix user to /opt/matrix (home dir). I launch the Docker container per instructions with $UID and $GID pointing to host machine matrix user so when i mount /opt/matrix/synapse to /data it has the same permissions to RW. 

Now when I launch the container it cant read the /etc/ssl/private/key.pem when I specify it under the homeserver.yaml (permission denied)

When I launch the docker container and go inside the container I can see it with root, ofcousre. But then I find out that /etc/passwd and /etc/group does not contain the UID and GID I have created and launched the docker container with. 

How can I make the privatekey readable without 0644?

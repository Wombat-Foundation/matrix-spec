---
number: 5840
title: "No create event in auth events - 3 matrix servers "
state: "closed"
author: "kamado-tanjiro-usa"
created_at: "2019-08-09T21:12:36Z"
updated_at: "2019-08-19T13:04:14Z"
url: "https://github.com/matrix-org/synapse/issues/5840"
labels: []
---


### Description


### Steps to reproduce

- set up 3 matrix synapse docker instances
- all listen to different ports 8008, 8009, and 8010 , within the docker instance, everything goes to 8008
 use ngrok to redirect ngrok https endpoints to listen to 8008 8009 8010 respectively. (
- all 3 matrix synapse docker instances share the same postgres database, hosted locally
- docker run them all. 
- I have 3 devices that log in each one of them
- I see messages initially but after more message exchanges, all 3 stop being able to send any. 


### Version information

the most current Riot iOS client available and Riot OSX client


- **Homeserver**: 

locally-hosted on docker

- **Version**: 

- **Install method**: 
1) install Docker

2) docker pull matrixdotorg/synapse

3) ```docker run -it --rm \
    --mount type=volume,src=synapse-data,dst=/data \
    -e SYNAPSE_SERVER_NAME=neo.zeon \
    -e SYNAPSE_REPORT_STATS=yes \
    matrixdotorg/synapse:latest generate
```


4) ```docker run -d --name synapse \
    --mount type=volume,src=synapse-data,dst=/data \
    -p 8008:8008 \
    -p 8448:8448 \
    matrixdotorg/synapse:latest
```

5) `docker cp homeserver.yaml  synapse:/data/homeserver.yaml `


6) docker restart synapse

7) to verify `docker logs synapse`


------

to run second instance

1)```docker run -it --rm \
    --mount type=volume,src=synapse2-data,dst=/data \
    -e SYNAPSE_SERVER_NAME=neo.zeon \
    -e SYNAPSE_REPORT_STATS=yes \
    matrixdotorg/synapse:latest generate
```


2) ```docker run -d --name synapse2 \
    --mount type=volume,src=synapse2-data,dst=/data \
    -p 8009:8008 \
    -p 8449:8448 \
    matrixdotorg/synapse:latest
```

3) `docker cp homeserver.yaml  synapse2:/data/homeserver.yaml `


4) docker restart synapse2




- **Platform**: 
homeserver is running on an iMAC

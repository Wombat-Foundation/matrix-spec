---
number: 11258
title: "Uploading a file fails with `M_UNKNOWN_TOKEN` `Access token has expired` but the same access token works with `/sync` and other API endpoints"
state: "closed"
author: "hafizworqapp"
created_at: "2021-11-05T09:35:43Z"
updated_at: "2023-06-28T06:01:16Z"
url: "https://github.com/matrix-org/synapse/issues/11258"
labels: []
---

When I do upload file via `https://synapse.api.worqapp.com/_matrix/media/r0/upload` with valid access token it shows an error  -> `401` `{"errcode":"M_UNKNOWN_TOKEN","error":"Access token has expired","soft_logout":true}`

But when calling for API `https://synapse.api.worqapp.com/_matrix/client/r0/sync?filter=%7B%22room%22%3A%7B%22state%22%3A%7B%22lazy_load_members%22%3Atrue%7D%7D%7D&set_presence=online&timeout=30000&since=s148768_997987_14124_382919_181953_6972_1_120454_1` (load member) with the same access token, it works no problem? why?

My code as below :

```
            val clientBuilder = OkHttpClient.Builder()
            //
            val authToken = "Bearer ".plus(SessionHolder.currentSession?.sessionParams?.credentials?.accessToken)

            Timber.d("Authtoken: $authToken")
            //Create a new Interceptor.
            val headerAuthorizationInterceptor = Interceptor { chain ->
                var request = chain.request()
                val headers = request.headers.newBuilder()
                    .add("Authorization", authToken)
                    .build()
                request = request.newBuilder().headers(headers).build()
                chain.proceed(request)
            }

            // OkHttp Logging interceptor
            val logging = HttpLoggingInterceptor()
            logging.level = HttpLoggingInterceptor.Level.BODY

            clientBuilder
                .addInterceptor(logging)
                .addInterceptor(headerAuthorizationInterceptor)

            val retrofit = Retrofit.Builder()
                .addCallAdapterFactory(RxJava2CallAdapterFactory.create())
                .addConverterFactory(GsonConverterFactory.create())
                .client(clientBuilder.build())
                .baseUrl(baseUrl)
                .build()

            return retrofit.create(UploadMatrixAPI::class.java)
```

calling function upload file

```
@POST("/_matrix/media/r0/upload")
    fun uploadImageToMatirxServer(@Body file: RequestBody): Observable<UploadImageMatrixResp>`

file UploadImageMatrixResp
`data class UploadImageMatrixResp(val content_uri: String?, val smsSent: String?)`

set datatype:
`var file = PathUtils.fileFromContentUri(applicationContext, contentUriOfFile)
            var mime = PathUtils.getMimeType(applicationContext, contentUriOfFile)

            if(file.exists() && ((file.length() / 1024) / 1024) >10) {
                Toast.makeText(this, "Attachment exceeds the 10MB limit", Toast.LENGTH_LONG).show()
                finish()
                return Unit
            }
            val disposable = uploadManualMedia(file, mime).subscribe ({ onFirstSuccess: UploadImageMatrixResp? ->
                val firstMediaUrl = onFirstSuccess?.content_uri
                Timber.d("Uri : $firstMediaUrl")
                //val content = buildMediaContent(mime, filteredClipData.size, bitmap, file, firstMediaUrl)
                //remainingClipData = filteredClipData
                //room?.sendEvent(EventType.MESSAGE, content.toContent())
            },
                {onError ->
                    onError.message?.let {

                    }
                })
```

function upload

```
private fun uploadManualMedia(file: File, mimeType:String): Observable<UploadImageMatrixResp>{
        val body =  file.asRequestBody(mimeType.toMediaTypeOrNull())
        return matrixAPI.uploadImageToMatirxServer(body)
            .subscribeOn(Schedulers.io())
            .observeOn(AndroidSchedulers.mainThread())
    }
```

initialize matrix lazy class

```
private val matrixAPI by lazy {
        UploadMatrixAPI.createWithBasicAuth(Constants.SYNAPSE_BASE_URL, applicationContext)
    }
```
Note: I try to received image from whatapps and I want to share image to my members in list of matrix member active. 

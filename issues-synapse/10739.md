---
number: 10739
title: "`txn` being redefined in for loop as we are iterating over it"
state: "closed"
author: "MadLittleMods"
created_at: "2021-09-01T19:28:37Z"
updated_at: "2021-09-03T09:59:25Z"
url: "https://github.com/matrix-org/synapse/issues/10739"
labels:
  - "S-Minor"
  - "T-Defect"
---

Spurred on by the note in https://github.com/matrix-org/synapse/pull/10734 about `txn` being redefined in for loop as we are iterating over it.

> We were originally iterating over `txn`, which can work, but as `txn` was being used to run a
simple_delete operation inside the loop, it was actually being redefined mid-loop.
>
> https://github.com/matrix-org/synapse/pull/10734

I was curious if there were other cases in the codebase, so I searched for `in txn:` and found one more case. It looks like this would cause a problem if there was an event where the `event_auth_chains` -> `chain_id` was not defined.

https://github.com/matrix-org/synapse/blob/c586d6803a2adebcdd486be2d9eac1f62fd7d4ab/synapse/storage/databases/main/events.py#L578-L588


## Linting

Maybe we should disallow iterating over `txn` directly in favor of using `txn.fetchall()`

---
number: 2003
title: "federation transaction queue gets wedged on a large queue of to-device messages"
state: "closed"
author: "richvdh"
created_at: "2017-03-14T23:11:07Z"
updated_at: "2017-03-15T13:08:45Z"
url: "https://github.com/matrix-org/synapse/issues/2003"
labels: []
---

If the number of to-device messages exceeds 100, then `get_new_device_msgs_for_remote` returns the oldest 100, and the `stream_id` of the newest of those 100, _as a `long`_.

This value is then stored in `last_device_list_stream_id_by_dest`, and is sent back into `get_new_device_msgs_for_remote` on the next loop. That calls `has_entity_changed`, which asserts that the `stream_id` is an `int` - hence the federation request is never sent.

